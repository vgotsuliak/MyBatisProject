<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ua.com.gotsuliak.firstproject.mapper.DepartmentMapper">
    <select id="getDepartment" parameterType="java.lang.Integer">
        select * from department where id = #{id}
    </select>
</mapper>